<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/home" app:showAsAction="ifRoom" android:enabled="true"
        android:icon="@drawable/home_nav_selector"
        tools:ignore="MenuTitle" />
    <item android:id="@+id/noti" app:showAsAction="ifRoom"
        android:icon="@drawable/noti_nav_selector"
        tools:ignore="MenuTitle" />
    <item android:id="@+id/commu" app:showAsAction="ifRoom"
        android:enabled="true" android:icon="@drawable/commu_nav_selector"
        tools:ignore="MenuTitle" />
    <item android:id="@+id/my" app:showAsAction="ifRoom"
        android:enabled="true" android:icon="@drawable/my_nav_selector"
        tools:ignore="MenuTitle" />
</menu>